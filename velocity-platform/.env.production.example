# =============================================================================
# Velocity AI Platform - Production Environment Configuration
# =============================================================================

NODE_ENV=production
VITE_APP_ENV=production

# Supabase Configuration - Production Project
VITE_SUPABASE_URL=https://your-production-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-production-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-production-service-role-key

# Database Configuration
DATABASE_URL=postgresql://postgres:your-production-password@db.your-production-project.supabase.co:5432/postgres
SUPABASE_DB_PASSWORD=your-production-database-password

# Authentication Configuration
JWT_SECRET_KEY=your-production-jwt-secret-very-secure
SUPABASE_JWT_SECRET=your-production-jwt-secret-very-secure

# Application Configuration
DEBUG=false
SECRET_KEY=your-production-app-secret-key-very-secure

# CORS Settings
CORS_ORIGINS=https://velocity.ai,https://www.velocity.ai,https://app.velocity.ai

# Real-time Features
ENABLE_REALTIME=true
REALTIME_ENDPOINT=https://your-production-project.supabase.co/realtime/v1

# File Storage Configuration
STORAGE_BUCKET=velocity-evidence-production
STORAGE_URL=https://your-production-project.supabase.co/storage/v1

# Email Configuration
EMAIL_PROVIDER=supabase
# Alternative: Use SendGrid for production emails
# EMAIL_PROVIDER=sendgrid
# SENDGRID_API_KEY=your-sendgrid-api-key
# FROM_EMAIL=noreply@velocity.ai

# AI Configuration (Production keys)
OPENAI_API_KEY=your-production-openai-key
ANTHROPIC_API_KEY=your-production-anthropic-key

# Cloud Integration Settings (Production accounts)
AWS_ACCESS_KEY_ID=your-production-aws-access-key
AWS_SECRET_ACCESS_KEY=your-production-aws-secret-key
AWS_DEFAULT_REGION=us-east-1

# Monitoring and Analytics
ENABLE_ANALYTICS=true
ANALYTICS_PROVIDER=supabase

# Logging Configuration
LOG_LEVEL=INFO
LOG_FORMAT=structured

# Rate Limiting (Production settings)
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=60

# Security Configuration
ENABLE_HTTPS_REDIRECT=true
ENABLE_SECURITY_HEADERS=true
SESSION_TIMEOUT_MINUTES=60

# Feature Flags (Production)
VITE_ENABLE_DEMO_MODE=false
VITE_SHOW_DEBUG_INFO=false
VITE_ENABLE_ANALYTICS=true

# Additional Production Security
VITE_ENABLE_CSP=true
VITE_ENABLE_HSTS=true
VITE_ENABLE_RATE_LIMITING=true

# =============================================================================
# Production Environment Setup Instructions
# =============================================================================

# 1. Create production Supabase project at https://supabase.com
# 2. Name it: velocity-production
# 3. Enable all security features and Row Level Security
# 4. Set up proper backup policies
# 5. Configure custom domain: velocity.ai
# 6. Set up SSL certificates
# 7. Configure CDN (Cloudflare/AWS CloudFront)
# 8. Set up monitoring and alerts
# 9. Test disaster recovery procedures

# Security Checklist:
# ✓ Enable RLS on all tables
# ✓ Set up proper IAM policies
# ✓ Configure CORS properly
# ✓ Enable HTTPS only
# ✓ Set up rate limiting
# ✓ Configure security headers
# ✓ Set up monitoring and alerts
# ✓ Regular security audits

# Deployment Commands:
# vercel --prod --env .env.production
# netlify deploy --prod --dir=dist --env .env.production