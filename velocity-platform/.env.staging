# =============================================================================
# Velocity AI Platform - Staging Environment Configuration
# =============================================================================

NODE_ENV=staging
VITE_APP_ENV=staging

# Supabase Configuration - Staging Project
VITE_SUPABASE_URL=https://your-staging-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-staging-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-staging-service-role-key

# Database Configuration
DATABASE_URL=postgresql://postgres:your-staging-password@db.your-staging-project.supabase.co:5432/postgres
SUPABASE_DB_PASSWORD=your-staging-database-password

# Authentication Configuration
JWT_SECRET_KEY=your-staging-jwt-secret
SUPABASE_JWT_SECRET=your-staging-jwt-secret

# Application Configuration
DEBUG=true
SECRET_KEY=your-staging-app-secret-key

# CORS Settings
CORS_ORIGINS=https://staging.velocity.ai,https://staging-velocity.vercel.app

# Real-time Features
ENABLE_REALTIME=true
REALTIME_ENDPOINT=https://your-staging-project.supabase.co/realtime/v1

# File Storage Configuration
STORAGE_BUCKET=velocity-evidence-staging
STORAGE_URL=https://your-staging-project.supabase.co/storage/v1

# Email Configuration
EMAIL_PROVIDER=supabase

# AI Configuration (Use test keys)
OPENAI_API_KEY=your-staging-openai-key
ANTHROPIC_API_KEY=your-staging-anthropic-key

# Cloud Integration Settings (Staging/Test accounts)
AWS_ACCESS_KEY_ID=your-staging-aws-access-key
AWS_SECRET_ACCESS_KEY=your-staging-aws-secret-key
AWS_DEFAULT_REGION=us-east-1

# Monitoring and Analytics
ENABLE_ANALYTICS=true
ANALYTICS_PROVIDER=supabase

# Logging Configuration
LOG_LEVEL=DEBUG
LOG_FORMAT=structured

# Rate Limiting (More relaxed for testing)
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=120

# Security Configuration
ENABLE_HTTPS_REDIRECT=true
ENABLE_SECURITY_HEADERS=true
SESSION_TIMEOUT_MINUTES=120

# Feature Flags
VITE_ENABLE_DEMO_MODE=true
VITE_SHOW_DEBUG_INFO=true
VITE_ENABLE_ANALYTICS=false

# =============================================================================
# Staging Environment Setup Instructions
# =============================================================================

# 1. Create staging Supabase project at https://supabase.com
# 2. Name it: velocity-staging
# 3. Update all environment variables above with staging values
# 4. Deploy to staging environment (Vercel, Netlify, etc.)
# 5. Test authentication flows
# 6. Test database operations
# 7. Validate integrations work with staging data

# Deployment Commands:
# vercel --env .env.staging
# netlify deploy --prod --dir=dist