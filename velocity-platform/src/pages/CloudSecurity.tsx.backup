import React, { useState } from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';
import { Progress } from '@/components/ui/progress';
import { useNavigate } from 'react-router-dom';
import { 
  Cloud, 
  Shield, 
  AlertTriangle, 
  CheckCircle, 
  Search,
  Activity,
  Lock,
  Database,
  Globe,
  Cpu,
  ArrowRight,
  TrendingUp,
  BarChart3,
  FileCheck,
  Settings,
  Zap
} from 'lucide-react';

export const CloudSecurity: React.FC = () => {
  const navigate = useNavigate();
  const [activeTab, setActiveTab] = useState('overview');

  const cloudProviders = [
    {
      name: 'AWS',
      icon: Globe,
      status: 'Connected',
      resources: 234,
      findings: 12,
      compliance: 94,
      lastScan: '5 minutes ago'
    },
    {
      name: 'Azure',
      icon: Cloud,
      status: 'Connected',
      resources: 156,
      findings: 8,
      compliance: 91,
      lastScan: '12 minutes ago'
    },
    {
      name: 'Google Cloud',
      icon: Database,
      status: 'Pending',
      resources: 0,
      findings: 0,
      compliance: 0,
      lastScan: 'Not started'
    }
  ];

  const securityFindings = [
    {
      severity: 'Critical',
      count: 2,
      color: 'from-red-500 to-red-600',
      examples: ['S3 bucket publicly accessible', 'RDS instance without encryption']
    },
    {
      severity: 'High',
      count: 8,
      color: 'from-orange-500 to-orange-600',
      examples: ['IAM policies too permissive', 'Security groups allow 0.0.0.0/0']
    },
    {
      severity: 'Medium',
      count: 15,
      color: 'from-yellow-500 to-yellow-600',
      examples: ['CloudTrail not configured', 'MFA not enforced']
    },
    {
      severity: 'Low',
      count: 32,
      color: 'from-blue-500 to-blue-600',
      examples: ['Tags missing', 'Old snapshots retained']
    }
  ];

  const complianceFrameworks = [
    { name: 'SOC 2', coverage: 92, findings: 8 },
    { name: 'ISO 27001', coverage: 88, findings: 12 },
    { name: 'PCI DSS', coverage: 95, findings: 5 },
    { name: 'HIPAA', coverage: 90, findings: 10 },
    { name: 'GDPR', coverage: 93, findings: 7 }
  ];

  return (
    <div className="container mx-auto p-6 max-w-7xl">
      {/* Header */}
      <div className="mb-8">
        <div className="flex items-center justify-between mb-6">
          <div>
            <h1 className="text-3xl font-bold text-slate-900 mb-2">Cloud Security Scanner</h1>
            <p className="text-slate-600">Multi-cloud security posture management and compliance monitoring</p>
          </div>
          <Button 
            size="lg"
            onClick={() => navigate('/tools/atlas')}
            className="bg-gradient-to-r from-blue-600 to-purple-600"
          >
            <Shield className="h-5 w-5 mr-2" />
            Run Security Scan
          </Button>
        </div>

        {/* Quick Stats */}
        <div className="grid grid-cols-1 md:grid-cols-4 gap-4 mb-6">
          <Card className="border-0 shadow-lg bg-gradient-to-br from-purple-50 to-blue-50">
            <CardContent className="p-6">
              <div className="flex items-center justify-between">
                <div>
                  <p className="text-sm text-slate-600 mb-1">Cloud Accounts</p>
                  <p className="text-2xl font-bold text-slate-900">3</p>
                </div>
                <Cloud className="h-8 w-8 text-purple-600" />
              </div>
            </CardContent>
          </Card>

          <Card className="border-0 shadow-lg bg-gradient-to-br from-red-50 to-orange-50">
            <CardContent className="p-6">
              <div className="flex items-center justify-between">
                <div>
                  <p className="text-sm text-slate-600 mb-1">Security Findings</p>
                  <p className="text-2xl font-bold text-slate-900">57</p>
                </div>
                <AlertTriangle className="h-8 w-8 text-red-600" />
              </div>
            </CardContent>
          </Card>

          <Card className="border-0 shadow-lg bg-gradient-to-br from-green-50 to-emerald-50">
            <CardContent className="p-6">
              <div className="flex items-center justify-between">
                <div>
                  <p className="text-sm text-slate-600 mb-1">Compliance Score</p>
                  <p className="text-2xl font-bold text-slate-900">92%</p>
                </div>
                <CheckCircle className="h-8 w-8 text-green-600" />
              </div>
            </CardContent>
          </Card>

          <Card className="border-0 shadow-lg bg-gradient-to-br from-blue-50 to-cyan-50">
            <CardContent className="p-6">
              <div className="flex items-center justify-between">
                <div>
                  <p className="text-sm text-slate-600 mb-1">Resources Scanned</p>
                  <p className="text-2xl font-bold text-slate-900">390</p>
                </div>
                <Search className="h-8 w-8 text-blue-600" />
              </div>
            </CardContent>
          </Card>
        </div>
      </div>

      {/* Main Content */}
      <Tabs value={activeTab} onValueChange={setActiveTab}>
        <TabsList className="grid w-full grid-cols-4 mb-6">
          <TabsTrigger value="overview">Overview</TabsTrigger>
          <TabsTrigger value="findings">Security Findings</TabsTrigger>
          <TabsTrigger value="compliance">Compliance</TabsTrigger>
          <TabsTrigger value="inventory">Cloud Inventory</TabsTrigger>
        </TabsList>

        <TabsContent value="overview" className="space-y-6">
          {/* Cloud Providers */}
          <Card className="border-0 shadow-lg">
            <CardHeader>
              <CardTitle>Connected Cloud Providers</CardTitle>
            </CardHeader>
            <CardContent>
              <div className="space-y-4">
                {cloudProviders.map((provider) => (
                  <div key={provider.name} className="flex items-center justify-between p-4 rounded-lg bg-slate-50">
                    <div className="flex items-center gap-4">
                      <div className={`p-3 rounded-lg €{
                        provider.status === 'Connected' ? 'bg-green-100' : 'bg-gray-100'
                      }`}>
                        <provider.icon className={`h-6 w-6 €{
                          provider.status === 'Connected' ? 'text-green-600' : 'text-gray-400'
                        }`} />
                      </div>
                      <div>
                        <h3 className="font-semibold text-slate-900">{provider.name}</h3>
                        <p className="text-sm text-slate-600">{provider.lastScan}</p>
                      </div>
                    </div>
                    <div className="flex items-center gap-6">
                      <div className="text-right">
                        <p className="text-sm text-slate-600">Resources</p>
                        <p className="font-semibold">{provider.resources}</p>
                      </div>
                      <div className="text-right">
                        <p className="text-sm text-slate-600">Findings</p>
                        <p className="font-semibold text-orange-600">{provider.findings}</p>
                      </div>
                      <div className="text-right">
                        <p className="text-sm text-slate-600">Compliance</p>
                        <p className="font-semibold text-green-600">{provider.compliance}%</p>
                      </div>
                      <Badge variant={provider.status === 'Connected' ? 'default' : 'secondary'}>
                        {provider.status}
                      </Badge>
                    </div>
                  </div>
                ))}
              </div>
              <Button variant="outline" className="w-full mt-4">
                <Cpu className="h-4 w-4 mr-2" />
                Add Cloud Provider
              </Button>
            </CardContent>
          </Card>

          {/* Security Overview */}
          <Card className="border-0 shadow-lg">
            <CardHeader>
              <CardTitle>Security Findings Summary</CardTitle>
            </CardHeader>
            <CardContent>
              <div className="grid grid-cols-1 md:grid-cols-4 gap-4">
                {securityFindings.map((finding) => (
                  <Card key={finding.severity} className="border-0 shadow-md">
                    <CardContent className="p-4">
                      <div className="flex items-center justify-between mb-2">
                        <Badge className={`bg-gradient-to-r €{finding.color} text-white`}>
                          {finding.severity}
                        </Badge>
                        <span className="text-2xl font-bold">{finding.count}</span>
                      </div>
                      <div className="space-y-1">
                        {finding.examples.map((example, idx) => (
                          <p key={idx} className="text-xs text-slate-600 line-clamp-1">
                            • {example}
                          </p>
                        ))}
                      </div>
                    </CardContent>
                  </Card>
                ))}
              </div>
            </CardContent>
          </Card>
        </TabsContent>

        <TabsContent value="findings" className="space-y-6">
          <Card className="border-0 shadow-lg">
            <CardHeader>
              <div className="flex items-center justify-between">
                <CardTitle>Security Findings</CardTitle>
                <div className="flex gap-2">
                  <Button variant="outline" size="sm">
                    <FileCheck className="h-4 w-4 mr-2" />
                    Export Report
                  </Button>
                  <Button variant="outline" size="sm">
                    <Settings className="h-4 w-4 mr-2" />
                    Configure Rules
                  </Button>
                </div>
              </div>
            </CardHeader>
            <CardContent>
              <div className="space-y-4">
                {/* Critical Findings */}
                <div className="space-y-2">
                  <h3 className="font-semibold text-red-600 flex items-center gap-2">
                    <AlertTriangle className="h-4 w-4" />
                    Critical Findings
                  </h3>
                  <div className="space-y-2">
                    <div className="p-4 rounded-lg bg-red-50 border border-red-200">
                      <div className="flex items-start justify-between">
                        <div>
                          <h4 className="font-semibold text-slate-900">S3 Bucket Publicly Accessible</h4>
                          <p className="text-sm text-slate-600 mt-1">
                            Bucket 'customer-data-prod' allows public read access
                          </p>
                          <div className="flex items-center gap-4 mt-2 text-sm text-slate-500">
                            <span>AWS • us-east-1</span>
                            <span>Found: 2 hours ago</span>
                          </div>
                        </div>
                        <Button size="sm" className="bg-red-600 hover:bg-red-700">
                          Fix Now
                        </Button>
                      </div>
                    </div>
                  </div>
                </div>

                {/* High Findings */}
                <div className="space-y-2">
                  <h3 className="font-semibold text-orange-600 flex items-center gap-2">
                    <AlertTriangle className="h-4 w-4" />
                    High Priority Findings
                  </h3>
                  <div className="space-y-2">
                    <div className="p-4 rounded-lg bg-orange-50 border border-orange-200">
                      <div className="flex items-start justify-between">
                        <div>
                          <h4 className="font-semibold text-slate-900">IAM Policy Too Permissive</h4>
                          <p className="text-sm text-slate-600 mt-1">
                            Policy 'DeveloperAccess' grants excessive permissions
                          </p>
                          <div className="flex items-center gap-4 mt-2 text-sm text-slate-500">
                            <span>AWS • Global</span>
                            <span>Found: 1 day ago</span>
                          </div>
                        </div>
                        <Button size="sm" variant="outline">
                          Review
                        </Button>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </CardContent>
          </Card>
        </TabsContent>

        <TabsContent value="compliance" className="space-y-6">
          <Card className="border-0 shadow-lg">
            <CardHeader>
              <CardTitle>Compliance Framework Coverage</CardTitle>
            </CardHeader>
            <CardContent>
              <div className="space-y-4">
                {complianceFrameworks.map((framework) => (
                  <div key={framework.name} className="space-y-2">
                    <div className="flex items-center justify-between">
                      <div className="flex items-center gap-2">
                        <h3 className="font-semibold">{framework.name}</h3>
                        <Badge variant="outline">{framework.findings} findings</Badge>
                      </div>
                      <span className="text-sm font-semibold text-green-600">
                        {framework.coverage}% compliant
                      </span>
                    </div>
                    <Progress value={framework.coverage} className="h-2" />
                  </div>
                ))}
              </div>
            </CardContent>
          </Card>
        </TabsContent>

        <TabsContent value="inventory" className="space-y-6">
          <Card className="border-0 shadow-lg">
            <CardHeader>
              <CardTitle>Cloud Resource Inventory</CardTitle>
            </CardHeader>
            <CardContent>
              <div className="text-center py-12">
                <Database className="h-12 w-12 text-slate-400 mx-auto mb-4" />
                <h3 className="text-lg font-semibold text-slate-900 mb-2">
                  Full Inventory Coming Soon
                </h3>
                <p className="text-slate-600">
                  Complete visibility into all your cloud resources across providers
                </p>
              </div>
            </CardContent>
          </Card>
        </TabsContent>
      </Tabs>

      {/* Integration CTA */}
      <Card className="mt-8 border-0 shadow-lg bg-gradient-to-r from-blue-600 to-purple-600 text-white">
        <CardContent className="p-8">
          <div className="flex items-center justify-between">
            <div>
              <h3 className="text-2xl font-bold mb-2">Connect More Cloud Providers</h3>
              <p className="opacity-90">
                Add GCP, OCI, and other cloud providers for complete security visibility
              </p>
            </div>
            <Button size="lg" className="bg-white text-blue-600 hover:bg-slate-100">
              <Zap className="h-5 w-5 mr-2" />
              Add Provider
            </Button>
          </div>
        </CardContent>
      </Card>
    </div>
  );
};