version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd velocity-platform
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: velocity-platform/dist
    files:
      - '**/*'
  cache:
    paths:
      - velocity-platform/node_modules/**/*