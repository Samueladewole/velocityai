version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd erip-platform
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: erip-platform/dist
    files:
      - '**/*'
  cache:
    paths:
      - erip-platform/node_modules/**/*